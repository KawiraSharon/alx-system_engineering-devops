CHANGE MASTER TO
  MASTER_HOST = '34.224.1.195',
  MASTER_USER = 'replication_user',
  MASTER_PASSWORD = '1234',
  MASTER_LOG_FILE = 'mysql-bin.000001',  -- Use the File value obtained from the master
  MASTER_LOG_POS = 797;               -- Use the Position value obtained from the master
